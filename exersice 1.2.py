# this is a program that prints out the even index of a string
name = input("what is your name")
# after having goten the name use the  len() function to get the no letterd in the string
y=int 
y= len(name)
# use this for loop to print out the even index of the name
for i  in range (0,y-1,2):
    print(name[i])
